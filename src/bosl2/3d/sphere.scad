include <BOSL2/std.scad>

// sphere(r=50);
// sphere(d=100);

// Ancoragem, rotacao e orientacao
// sphere(d=100, anchor=FRONT);
// sphere(d=100, anchor=FRONT, spin=30);

sphere(d=100, anchor=BOTTOM, orient=RIGHT+TOP);
